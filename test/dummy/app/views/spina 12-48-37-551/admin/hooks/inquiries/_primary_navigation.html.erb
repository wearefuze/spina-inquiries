<%= render Spina::MainNavigation::SubNavComponent.new(:inquiries) do |nav| %>
  <% nav.icon do %>
    <%= heroicon('inbox', style: :solid, class: 'w-8 h-8 text-white md:mr-3') %>

    <div class="text-white font-semibold hidden md:block transform -translate-x-2 ease-in-out duration-300 absolute md:relative opacity-0 transition-all" data-navigation-target="label">
      Inquiries
    </div>
  <% end %>

  <% nav.links do %>
    <%= render Spina::MainNavigation::LinkComponent.new("Inquiries", spina.admin_inquiries_path,
      active: request.path.start_with?("/#{Spina.config.backend_path}/inquiries")
    ) %>
  <% end %>
<% end %>